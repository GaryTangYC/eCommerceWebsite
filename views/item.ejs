<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />

    <title>Rocket eCommerce</title>
  </head>

  <!-- <header><%- include('./sidebars.ejs'); %></header> -->

  <body class="text-center bg-light text-dark">
    <!-- Optional JavaScript; choose one of the two! -->

    <!-- Option 1: Bootstrap Bundle with Popper -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
      crossorigin="anonymous"
    ></script>
    <p><strong>Home Page - All Items</strong></p>
    <div class="card" style="width: 18rem">
      <img
        class="card-img-top"
        src="/images/<%= itemDetails[0].product_name %>.jpg"
        alt="Card image cap"
      />
      <div class="card-body">
        <p class="card-text">
          <strong>
            <%= itemDetails[0].product_name %> <br />
            $<%= itemDetails[0].price %></strong
          >
        </p>
        <p class="card-text">
          Description: <%= itemDetails[0].summary%> <br />
          Weight: <%= itemDetails[0].weight%>
        </p>
        <form
          action="/item/<%= itemDetails[0].id %>"
          method="POST"
          onsubmit="return confirm('Item will be added to cart - please confirm');"
        >
          <% console.log(itemDetails) %>
          <input
            type="hidden"
            id="itemID"
            name="itemID"
            value="<%= itemDetails[0].id %> "
          />
          <input type="submit" value="Add Item" />
        </form>
      </div>
    </div>
  </body>
</html>
